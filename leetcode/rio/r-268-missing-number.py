from typing import *

def missing_number(nums: List[int]) -> int:
    """
        :type nums: List[int]
        :rtype: int
    """
    
    # low = 0
    # high = len(nums) - 1
    # while low != high:
    #     mid = (low + high) // 2
    n = len(nums)
