from typing import *

def missing_number(nums: List[int]) -> int:
    """
        :type nums: List[int]
        :rtype: int
    """
    
    n = len(nums) + 1
    low = 0
    high = n - 1
    for i in range(n):
        
    # while low != high:
        # mid = (low + high) // 2
        
    